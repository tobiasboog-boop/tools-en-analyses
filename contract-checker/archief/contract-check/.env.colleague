# ============================================================================
# Contract Checker - Environment Configuration Template
# ============================================================================
# INSTRUCTIES VOOR COLLEGA:
# 1. Kopieer dit bestand naar: .env
# 2. Vraag Mark om de echte API keys (via veilig kanaal!)
# 3. Vul de keys in hieronder
# ============================================================================

# Database Configuration (PostgreSQL - Legacy)
DB_HOST=10.3.152.9
DB_PORT=5432
DB_NAME=1210
DB_USER=postgres
DB_PASSWORD=VRAAG_MARK           # ← Vraag echte password
DB_SCHEMA=contract_checker

# Supabase Configuration (Centrale Notifica App Database)
SUPABASE_URL=https://usxstdmeljiclmcbjgvu.supabase.co
SUPABASE_ANON_KEY=VRAAG_MARK     # ← Vraag anon key
SUPABASE_SERVICE_KEY=VRAAG_MARK  # ← Vraag service key

# LLM Provider API Keys
# PILOT: We gebruiken nu alleen Claude (Mistral komt later)
ANTHROPIC_API_KEY=VRAAG_MARK     # ← Vraag Claude API key
MISTRAL_API_KEY=                 # ← Leeg laten voor pilot
OPENAI_API_KEY=                  # ← Leeg laten (niet nodig)

# Feature Flags
USE_NEW_LLM_SYSTEM=true          # ← Nieuwe LLM systeem ENABLED

# Contracts Storage (OneDrive synced folder)
# BELANGRIJK: Pas aan naar jouw lokale pad!
CONTRACTS_FOLDER=C:/Users/JOUW_NAAM/path/to/contracts

# VPS4 SSH Configuration (voor deployment - niet nodig voor local testing)
VPS4_HOST=212.132.90.158
VPS4_USER=root
VPS4_SSH_KEY_PATH=C:/Users/JOUW_NAAM/path/to/ssh/key
VPS4_SSH_PASSPHRASE=VRAAG_MARK

# ============================================================================
# HUIDIGE STATUS (Pilot):
# ============================================================================
# LLM Configuration:
#   - contract_generation      → Claude Sonnet 4 (Cloud)
#   - werkbon_classification   → Claude Sonnet 4 (Cloud)
#
# Beide acties gebruiken nu Claude (geen Mistral API key nodig voor pilot)
#
# Token usage wordt getracked in Supabase: llm_usage_logs tabel
# ============================================================================

# ============================================================================
# LATER (Week 2-3): Mistral Toevoegen voor Cost Savings
# ============================================================================
# 1. Krijg Mistral API key: https://console.mistral.ai/
# 2. Vul in: MISTRAL_API_KEY=sk-...
# 3. Run in Supabase SQL Editor:
#
#    UPDATE llm_configurations
#    SET model_id = (SELECT id FROM llm_models WHERE model_code = 'mistral-large-latest')
#    WHERE action_type = 'werkbon_classification' AND organization_id IS NULL;
#
# 4. Restart app → 76% cost savings! 🎉
# ============================================================================
